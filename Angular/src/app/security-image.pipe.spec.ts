import { SecurityImagePipe } from './security-image.pipe';

describe('SecurityImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SecurityImagePipe();
    expect(pipe).toBeTruthy();
  });
});
